spring:
  application:
    name: userservice
  datasource:
    url: jdbc:postgresql://localhost:5732/fitness-micro-user
    username: postgres
    password : postgres

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect


